# Homelab Infrastructure Packages
#
# This file installs the local packages with their dependencies.
# Individual dependencies are now managed in each package's pyproject.toml.

# Install in order (common is a dependency of conf-gen)
-e ./common
-e ./conf-gen

# Legacy individual dependencies (for OpenWRT uci-defaults script compatibility)
# These are now declared in package pyproject.toml files but listed here for @include: expansion
cryptography
emoji
ifaddr
packaging
pycountry
pytz
pyyaml
requests
